[phases.setup]
nixPkgs = ["python3", "python312Packages.pip", "ruby"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt", "bundle install"]

[start]
cmd = "python3 bot.py & ruby myapp.rb"
